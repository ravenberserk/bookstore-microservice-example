server:
  servlet:
    context-path: /auth

spring:
  datasource:
    url: jdbc:mysql://authdb:3306/${AUTHDB_DB}?useLegacyDatetimeCode=false&serverTimezone=UTC
    username: ${AUTHDB_USER}
    password: ${AUTHDB_PASSWORD}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect