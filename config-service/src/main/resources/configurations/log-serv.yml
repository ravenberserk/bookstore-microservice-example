server:
  port: 3333
  
spring:
  cloud:
    stream:
      bindings:
        input:
          destination: logExchange
          group: log-queue
      rabbit:
        bindings:
          input:
            consumer:
              bindingRoutingKey: "*.created"
              exchangeType: topic